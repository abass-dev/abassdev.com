version: '3.8'

services:
  app:
    build: .
    ports:
      - "3005:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - abassdev:/app/.next
    environment:
      - NODE_ENV=development

volumes:
  abassdev:


